# Core framework
Django==6.0.5

# REST API
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1

# Frontend CORS
django-cors-headers==4.9.0

# Configuration / secrets
python-dotenv==1.2.2

# OAuth token encryption at rest (Fernet)
cryptography==48.0.0

# OAuth callback + GitHub API client
requests==2.34.2

# OIDC: id_token verification (Google JWKS, JWT decoding)
authlib==1.6.5

# Tests
pytest==8.4.2
pytest-django==4.11.1
pytest-mock==3.15.0

# Lint / format
ruff==0.14.0

# Type stubs for Django (Pyright/Pylance — silences ORM false positives)
django-stubs==6.0.4
